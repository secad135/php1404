در این جزوه می‌خواهیم مرحله **Create (ایجاد رکورد جدید)** از پروژه CRUD را با زبان **PHP + MySQL** به صورت **رویه‌ای (Procedural)** و بسیار ساده آموزش دهیم.

---

# 🎯 هدف این درس

یاد بگیریم چگونه:

1. به دیتابیس MySQL وصل شویم
2. یک فرم HTML طراحی کنیم
3. اطلاعات فرم را دریافت کنیم
4. در دیتابیس ذخیره کنیم

---

# 🧱 مرحله اول: ساخت دیتابیس و جدول در MySQL

ابتدا در phpMyAdmin یا محیط MySQL این دستورات را اجرا کنید:

```sql
CREATE DATABASE school_db CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;

USE school_db;

CREATE TABLE students (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    family VARCHAR(50) NOT NULL,
    nomre_php FLOAT NOT NULL
);
```

## توضیح:

* `id` → شماره رکورد (خودکار افزایش پیدا می‌کند)
* `name` → نام دانش آموز
* `family` → نام خانوادگی
* `nomre_php` → نمره درس PHP

---

# 🗂 ساختار پوشه پروژه

داخل پوشه `htdocs` یا `www` یک پوشه بسازید مثلا:

```
crud_project/
    index.php
    insert.php
```

---

# 🧩 مرحله دوم: طراحی فرم (index.php)

فایل `index.php` را بسازید:

```php
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>ثبت دانش آموز</title>
</head>
<body>

<h2>فرم ثبت دانش آموز</h2>

<form action="insert.php" method="post">
    
    نام:
    <input type="text" name="name" required>
    <br><br>

    نام خانوادگی:
    <input type="text" name="family" required>
    <br><br>

    نمره PHP:
    <input type="number" step="0.01" name="nomre_php" required>
    <br><br>

    <input type="submit" value="ثبت اطلاعات">

</form>

</body>
</html>
```

---

## 📌 توضیح کامل فرم

### `<form action="insert.php" method="post">`

* `action` → مشخص می‌کند اطلاعات به کدام فایل ارسال شود.
* `method="post"` → اطلاعات به صورت مخفی ارسال می‌شود (برای ثبت اطلاعات بهتر است از POST استفاده کنیم).

---

### input ها:

| نوع    | کاربرد     |
| ------ | ---------- |
| text   | گرفتن نام  |
| number | گرفتن نمره |
| submit | دکمه ارسال |

---

# 🧩 مرحله سوم: ذخیره اطلاعات در دیتابیس (insert.php)

فایل `insert.php` را بسازید:

```php
<?php

// 1️⃣ اتصال به دیتابیس
$connection = mysqli_connect("localhost", "root", "", "school_db");

// بررسی اتصال
if (!$connection) {
    die("خطا در اتصال به دیتابیس: " . mysqli_connect_error());
}


// 2️⃣ دریافت اطلاعات فرم
$name = $_POST['name'];
$family = $_POST['family'];
$nomre_php = $_POST['nomre_php'];


// 3️⃣ نوشتن دستور SQL
$sql = "INSERT INTO students (name, family, nomre_php)
        VALUES ('$name', '$family', '$nomre_php')";


// 4️⃣ اجرای دستور
$result = mysqli_query($connection, $sql);


// 5️⃣ بررسی نتیجه
if ($result) {
    echo "اطلاعات با موفقیت ثبت شد.";
} else {
    echo "خطا در ثبت اطلاعات: " . mysqli_error($connection);
}


// 6️⃣ بستن اتصال
mysqli_close($connection);

?>
```

---

# 🔎 توضیح کامل توابع استفاده شده

---

## ✅ 1️⃣ mysqli_connect()

```php
mysqli_connect("localhost", "root", "", "school_db");
```

### پارامترها:

1. نام سرور → localhost
2. نام کاربری → root
3. رمز عبور → (در XAMPP معمولا خالی است)
4. نام دیتابیس

اگر اتصال موفق نباشد مقدار `false` برمی‌گرداند.

---

## ✅ 2️⃣ mysqli_connect_error()

اگر اتصال مشکل داشته باشد پیام خطا را نمایش می‌دهد.

---

## ✅ 3️⃣ $_POST

آرایه‌ای آماده در PHP برای گرفتن اطلاعات فرم با method=post

مثال:

```php
$_POST['name']
```

یعنی مقدار input که name آن برابر name است.

---

## ✅ 4️⃣ INSERT INTO

ساختار دستور افزودن رکورد:

```sql
INSERT INTO table_name (column1, column2)
VALUES (value1, value2);
```

---

## ✅ 5️⃣ mysqli_query()

```php
mysqli_query($connection, $sql);
```

این تابع دستور SQL را اجرا می‌کند.

اگر موفق باشد → true
اگر خطا باشد → false

---

## ✅ 6️⃣ mysqli_error()

اگر در اجرای دستور مشکلی باشد پیام خطا را نمایش می‌دهد.

---

## ✅ 7️⃣ mysqli_close()

اتصال به دیتابیس را می‌بندد.

---

# 🧪 تمرین برای دانش آموزان

1. یک ستون جدید به نام `mobile` به جدول اضافه کنید و فرم را تغییر دهید.
2. اگر نمره بیشتر از 20 وارد شد پیام خطا بدهد.
3. بعد از ثبت موفق، کاربر را دوباره به فرم برگردانید.

---

# 🎓 جمع‌بندی مرحله Create

ما یاد گرفتیم:

✔ ساخت جدول
✔ طراحی فرم
✔ دریافت اطلاعات با $_POST
✔ اجرای INSERT
✔ بررسی خطا
✔ بستن اتصال

---
