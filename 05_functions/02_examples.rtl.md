# 🎯 توابع پرکاربرد آرایه در PHP

### 1. **count()**

برای شمارش تعداد عناصر آرایه.

```php
$fruits = ["apple", "banana", "orange"];
echo count($fruits); // خروجی: 3
```

---

### 2. **array_push()**

افزودن عنصر به انتهای آرایه.

```php
$numbers = [1, 2, 3];
array_push($numbers, 4, 5);
print_r($numbers);
// خروجی: [1, 2, 3, 4, 5]
```

---

### 3. **array_pop()**

حذف آخرین عنصر آرایه و برگرداندن آن.

```php
$names = ["Ali", "Sara", "Reza"];
$last = array_pop($names);
echo $last; // Reza
```

---

### 4. **array_shift()**

حذف اولین عنصر آرایه و برگرداندن آن.

```php
$colors = ["red", "green", "blue"];
$first = array_shift($colors);
echo $first; // red
```

---

### 5. **array_unshift()**

افزودن یک یا چند عنصر به ابتدای آرایه.

```php
$cars = ["BMW", "Toyota"];
array_unshift($cars, "Benz", "Mazda");
print_r($cars);
// ["Benz", "Mazda", "BMW", "Toyota"]
```

---

### 6. **in_array()**

بررسی وجود یک مقدار در آرایه.

```php
$animals = ["cat", "dog", "lion"];
if (in_array("dog", $animals)) {
    echo "Yes, dog is here!";
}
```

---

### 7. **array_merge()**

ادغام چند آرایه با هم.

```php
$a = [1, 2, 3];
$b = [4, 5, 6];
$merged = array_merge($a, $b);
print_r($merged); // [1, 2, 3, 4, 5, 6]
```

---

### 8. **array_slice()**

بریدن (گرفتن بخشی) از آرایه.

```php
$nums = [10, 20, 30, 40, 50];
$part = array_slice($nums, 1, 3);
print_r($part); // [20, 30, 40]
```

---

### 9. **array_reverse()**

برگرداندن ترتیب عناصر آرایه.

```php
$letters = ["A", "B", "C"];
print_r(array_reverse($letters)); // ["C", "B", "A"]
```

---

### 10. **sort()**

مرتب‌سازی آرایه به ترتیب صعودی (از کوچک به بزرگ).

```php
$nums = [4, 2, 8, 1];
sort($nums);
print_r($nums); // [1, 2, 4, 8]
```

---

### 11. **rsort()**

مرتب‌سازی نزولی.

```php
$nums = [4, 2, 8, 1];
rsort($nums);
print_r($nums); // [8, 4, 2, 1]
```

---

### 12. **array_sum()**

جمع عناصر عددی آرایه.

```php
$numbers = [5, 10, 15];
echo array_sum($numbers); // 30
```

---

### 13. **array_unique()**

حذف عناصر تکراری از آرایه.

```php
$nums = [1, 2, 2, 3, 3, 3];
print_r(array_unique($nums)); // [1, 2, 3]
```

---

### 14. **array_keys()** و **array_values()**

دریافت کلیدها یا مقدارهای آرایه‌ی انجمنی.

```php
$student = ["name" => "Ali", "age" => 18, "grade" => "A"];
print_r(array_keys($student));   // ["name", "age", "grade"]
print_r(array_values($student)); // ["Ali", 18, "A"]
```

---

## 1️⃣ **ksort()** – مرتب‌سازی کلیدها به صورت صعودی (A → Z)

این تابع آرایه را بر اساس **کلیدها** (نه مقادیر) از کوچک به بزرگ مرتب می‌کند.

### مثال:

```php
$student = [
    "c" => 20,
    "a" => 15,
    "b" => 18
];

ksort($student);
print_r($student);
```

### خروجی:

```
Array
(
    [a] => 15
    [b] => 18
    [c] => 20
)
```

---

## 2️⃣ **krsort()** – مرتب‌سازی کلیدها به صورت نزولی (Z → A)

این تابع کلیدهای آرایه را از بزرگ به کوچک مرتب می‌کند.

### مثال:

```php
$student = [
    "c" => 20,
    "a" => 15,
    "b" => 18
];

krsort($student);
print_r($student);
```

### خروجی:

```
Array
(
    [c] => 20
    [b] => 18
    [a] => 15
)
```
